const generateManager = (name, id, email, officeNumber) => {};

const generateEngineer = (name, id, email, githubUsername) => {};

const generateIntern = (name, id, email, school) => {};

// function generateEmployees

// function generateHTML
